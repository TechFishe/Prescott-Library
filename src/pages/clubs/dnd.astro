---
    import Layout from "@layouts/Layout.astro";
    const URL:string = Astro.url.pathname.split("/").join(" ");
    const URL_two:string = URL.split(" ");
---

<Layout title="Clubs | DnD" bigText="Dnd">
    {URL_two[0]}
</Layout>
