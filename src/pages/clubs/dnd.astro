---
    import Layout from "@layouts/Layout.astro";
---

<Layout title="Clubs | DnD" bigText="Dnd">
    {Astro.url.pathname.split("/").map((item, key) => (
        <span>{item}<br /><span>
    ))}
</Layout>
